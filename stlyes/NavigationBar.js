import { StyleSheet } from 'react-native';

export const NavigationStlyes = StyleSheet.create({ 
    Bar: {
        marginTop: 30,
        padding: 20
    },
    Button: {
        backgroundColor: '#000',
        borderColor: '#000',
    }
});